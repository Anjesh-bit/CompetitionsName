import { AdminAddCompetition } from './admin-add-competition';

describe('AdminAddCompetition', () => {
  it('should create an instance', () => {
    expect(new AdminAddCompetition()).toBeTruthy();
  });
});
